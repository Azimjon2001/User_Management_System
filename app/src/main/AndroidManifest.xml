<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.electsofte.parser">
    <uses-permission android:name="android.permission.INTERNET">

    </uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Contacts"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.mainactivity"
        tools:targetApi="m">
        <activity android:name=".users.MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".users.post.User_Activity"
            android:theme="@style/Theme.Parser"/>
        <activity android:name=".users.get_delete_update.UsersListGET"
            android:theme="@style/Theme.Parser"/>
        <activity android:name=".users.get_delete_update.UserDetailsActyvity"
            android:theme="@style/Theme.Parser"/>
        <activity android:name=".users.get_delete_update.User_activity_PUT"
            android:theme="@style/Theme.Parser"/>
    </application>

</manifest>